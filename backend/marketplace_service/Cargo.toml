[package]
name = "marketplace_service"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sqlx = { version = "0.7", features = ["runtime-tokio", "postgres", "chrono", "uuid"] }
chrono = { version = "0.4", features = ["serde"] }
log = "0.4"
uuid = { version = "1.0", features = ["v4", "serde"] }
tracing = "0.1"
async-trait = "0.1"
# Used by revenue_protection.rs (contact masking + message fingerprinting) but
# never declared, so this crate could not compile at all.
regex = "1.11.1"
md5 = "0.7.0"
shared = { path = "../shared" }
auth_service = { path = "../auth_service" }

[dev-dependencies]
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }
