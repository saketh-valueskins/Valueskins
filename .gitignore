node_modules/
.env
.env.local
.env.*
.DS_Store
backend/target/
**/target/
.vercel
.env*.local
*.tsbuildinfo

# stale backup of node_modules — never ship, never track
node_modules.bak/

# secrets — never commit
CREDENTIALS.md
SUPABASE_CREDENTIALS.md
